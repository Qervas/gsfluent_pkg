/**
 * SplatScene — renders the active 3DGS splats inside the R3F scene.
 *
 * Stub for Task 3.1; full implementation lands in Task 3.2 using the
 * @mkkellogg/gaussian-splats-3d API surface discovered in the Phase 0
 * spike (DropInViewer + dynamicScene:true + splatDataTextures.baseData
 * .centers + updateDataTexturesFromBaseData).
 */
export function SplatScene() {
  return null;
}
